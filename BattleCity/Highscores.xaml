<Page
    x:Class="BattleCity.Highscores"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BattleCity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:Name="MenuButton"
                Click="MenuButton_Click"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Width="150"
                Height="60" FontSize="20" FontFamily="Impact" 
                Content="Back to Menu"
               />
        <Button x:Name="HSButton"
                Click="HSButton_Click"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Center"
                Width="150"
                Height="60" FontSize="20" FontFamily="Impact" 
                Content="Show highscores" Margin="565,0,565,660"
               />
        <TextBlock x:Name="HStext" HorizontalAlignment="Left" Margin="565,110,0,0" TextWrapping="Wrap" Text="Press button to show highscores" VerticalAlignment="Top" Height="102" Width="218"/>
    </Grid>
</Page>
